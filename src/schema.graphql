# import User from './generated/prisma.graphql'
# import Product from './generated/prisma.graphql'
# import Cart from './generated/prisma.graphql'

type Query {
  user(id: ID!): User
  products(searchString: String): [Product]
  carts: [Cart]
}

type Mutation {
  createCart(productId: ID!): Cart
  deleteCart(id: ID!): Cart
}
